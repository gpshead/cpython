Remove the 3.11.0a3 added ``max_tasks_per_child`` from
:class:`concurrent.futures.ProcessPoolExecutor` as the implementation relied
on mixing threads+`fork()` on many systems which can cause deadlocks.
